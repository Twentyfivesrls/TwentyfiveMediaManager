app:
  file-security:
    storage-root: /opt/app/files
    max-upload-size-bytes: 10485760

    allowed-content-types:
      - image/jpeg
      - image/png
      - image/webp
      - application/pdf

    caller-policies:
      - caller: "fidelity25.it"
        roots:
          - "twentyfive-fidelity"

      - caller: "anticapasticceriarende.it"
        roots:
          - "apa"

      - caller: "https://anticapasticceriarende.it/"
        roots:
          - "apa"

      - caller: "www.anticapasticceriarende.it"
        roots:
          - "apa"
logging:
  level:
    root: INFO
    com.example.twentyfivemediamanager: DEBUG
    org.springframework.security: INFO